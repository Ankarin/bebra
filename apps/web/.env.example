# Database (any Postgres - local, Neon, Supabase, Railway, etc.)
DATABASE_URL=postgres://user:password@localhost:5432/glaife

# Admin auth (simple password for dashboard access)
ADMIN_PASSWORD=change-me-in-production
